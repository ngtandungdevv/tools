services:
  - type: web
    name: dev-tools-web
    env: python
    buildCommand: "./.render-build.sh"
    startCommand: "gunicorn app:app -b 0.0.0.0:$PORT --timeout 120"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: NODE_ENV
        value: production
